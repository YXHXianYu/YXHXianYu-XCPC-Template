#include<bits/stdc++.h>
using namespace std;
#define int long long

void work() {
    int n, m;
    cin >> n >> m;

    cout << "1/" << n << endl;
}

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t = 1;
    // cin >> t;
    while(t--) {
        work();
    }

    return 0;
}